export class App {

    name: string;
    position: number;
    weight: number;
    symbol: string;

    constructor(name: string, position: number, weight: number, symbol: string) {
        this.name = name;
        this.position = position;
        this.weight = weight;
        this.symbol = symbol;
    }
}